---
# - name: Exercise #1
#   hosts: localhost

#   roles:
#    - print

  - name: Exercise for setting up a web server in an EC2 #2
    hosts: all
    user: ec2-user
    become: true
    become_method: sudo
    become_user: root

    roles:
     - setup